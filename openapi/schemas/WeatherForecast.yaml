type: object

additionalProperties: false

required:
  - date
  - temperatureC
  - summary

properties:
  date:
    type: string
    format: date-time
    description: The date of the weather forecast

  temperatureC:
    type: integer
    format: int32

  summary:
    type: string
