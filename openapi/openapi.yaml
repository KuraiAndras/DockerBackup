openapi: 3.0.0
servers:
  - url: https://localhost:7189
info:
  title: DockerBackup
  version: 1.0.0
  license:
    name: MIT
    url: https://github.com/KuraiAndras/DockerBackup/blob/main/LICENSE
security: []

paths:
  /api/weatherforecast:
    get:
      summary: Get weather forecast
      description: Get the weather forecast for the next 5 days
      operationId: GetWeatherForecast
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "./schemas/WeatherForecast.yaml"
